### chmod permissions to file in GitHub?

- To set the flag, use the following command:
```
git update-index --chmod=+x path/to/file
```

- To remove it, use:
```
git update-index --chmod=-x path/to/file
```
