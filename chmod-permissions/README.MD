### chmod permissions to file in Git?

- To set the flag, use following command:
```
git update-index --chmod=+x path/to/file
```

- To remove it, use:
```
git update-index --chmod=-x path/to/file
```
